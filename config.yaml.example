server:
  port: 8080
  host: "0.0.0.0"

database:
  # DSN (Data Source Name) connection string
  # Example: postgres://user:password@localhost:5432/ns116?sslmode=disable
  dsn: "postgres://ns116:ns116pass@localhost:5432/ns116?sslmode=disable"

aws:
  access_key_id: "AKIA****************"
  secret_access_key: "********************************"
  region: "us-east-1"

# Only these zones will be visible and editable.
# If empty or omitted, ALL zones in the account will be listed.
hosted_zones: []
#  - id: "****************"
#    label: "example.com"
#  - id: "****************"
#    label: "internal.corp"

#oidc:
#  enabled: true
#  providers:
#    pocketid:
#      label: "Pocket ID"
#      issuer_url: "https://pocket-id.example.com"
#      client_id: "ns116"
#      # Pocket ID can be configured as a public client (no secret) or confidential client.
#      # client_secret: "optional"
#      redirect_url: "https://ns116.example.com/oidc/callback"
#      scopes: ["openid", "profile", "email", "groups"]
#      username_claim: "preferred_username"
#      groups_claim: "groups"
#      # Map OIDC groups to NS116 roles; login is denied if no group matches.
#      group_mapping:
#        admin: ["ns116-admins"]
#        editor: ["ns116-editors"]
#      username_claim: "preferred_username"
#    google:
#      label: "Google"
#      issuer_url: "https://accounts.google.com"
#      client_id: "YOUR_GOOGLE_CLIENT_ID"
#      client_secret: "YOUR_GOOGLE_CLIENT_SECRET"
#      redirect_url: "https://ns116.example.com/oidc/callback"
#      scopes: ["openid", "profile", "email"]
#      username_claim: "email"
#      groups_claim: "groups"
#      group_mapping:
#        editor: ["ns116-editors"]
#      extra_auth_params:
#        access_type: "offline"
#        prompt: "consent"

#ldap:
#  enabled: true
#  url: "ldap://ldap.forumsys.com:389"
#  bind_dn: "cn=read-only-admin,dc=example,dc=com"
#  bind_password: "password"
#  base_dn: "dc=example,dc=com"
#  user_filter: "(uid=%s)"
#  username_attr: "uid"
#  starttls: false
#  skip_verify: false
#  # Optional: Filter to find groups. %s = User DN, %u = User Login (UID)
#  # Default (Standard LDAP): (|(member=%s)(uniqueMember=%s))
#  # POSIX Groups (memberUid): (&(objectClass=posixGroup)(memberUid=%u))
#  group_filter: ""
#  group_mapping:
#    # Users in 'mathematicians' get admin access (e.g. riemann, gauss)
#    admin: "ou=mathematicians,dc=example,dc=com"
#    # Users in 'scientists' get editor access (e.g. einstein, tesla)
#    editor: "ou=scientists,dc=example,dc=com"